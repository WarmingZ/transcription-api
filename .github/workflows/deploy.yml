name: Deploy API

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ secrets.SERVER_PORT }}
        script: |
          echo "🚀 Деплой API..."
          
          # Переходимо в домашню директорію
          cd ~
          
          # Створюємо директорію проекту
          mkdir -p transcription-api
          cd transcription-api
          
          # Клонуємо репозиторій якщо немає
          if [ ! -d ".git" ]; then
            git clone https://github.com/WarmingZ/transcription-api.git .
          else
            git pull origin main
          fi
          
          # Встановлюємо системні залежності (без sudo)
          apt-get update -qq
          apt-get install -y python3 python3-pip python3-venv ffmpeg libsndfile1
          
          # Створюємо venv
          python3 -m venv venv
          source venv/bin/activate
          
          # Встановлюємо Python пакети
          pip install --upgrade pip -q
          pip install -r requirements.txt -q
          
          # Завантажуємо моделі
          python3 download_models.py || echo "⚠️ Моделі вже завантажені"
          
          # Зупиняємо старий процес
          pkill -f "python3 main.py" || true
          sleep 2
          
          # Запускаємо новий
          nohup python3 main.py > logs/app.log 2>&1 &
          echo $! > logs/app.pid
          
          # Чекаємо
          sleep 10
          
          # Перевіряємо
          if curl -f http://localhost:8000/health; then
            echo "✅ API працює!"
            echo "🌐 http://176.9.9.106:8000"
            echo "📁 Проект в: ~/transcription-api"
            echo "📋 Логи: ~/transcription-api/logs/app.log"
          else
            echo "❌ Помилка запуску"
            cat logs/app.log
            exit 1
          fi