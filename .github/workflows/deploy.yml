name: Deploy API

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ secrets.SERVER_PORT }}
        script: |
          echo "🚀 Збірка та запуск API..."
          
          # Переходимо в директорію проекту
          cd /opt/transcription-api
          
          # Оновлюємо код з GitHub
          git pull origin main
          
          # Встановлюємо залежності
          sudo apt-get update -qq
          sudo apt-get install -y python3 python3-pip python3-venv ffmpeg libsndfile1
          
          # Створюємо venv якщо немає
          if [ ! -d "venv" ]; then
            python3 -m venv venv
          fi
          
          # Активуємо venv та встановлюємо пакети
          source venv/bin/activate
          pip install --upgrade pip -q
          pip install -r requirements.txt -q
          
          # Завантажуємо моделі якщо немає
          if [ ! -d "models/faster-whisper-medium" ]; then
            python3 download_models.py || echo "⚠️ Моделі вже завантажені"
          fi
          
          # Зупиняємо старий процес
          pkill -f "python3 main.py" || true
          sleep 2
          
          # Запускаємо новий
          nohup python3 main.py > logs/app.log 2>&1 &
          echo $! > logs/app.pid
          
          # Чекаємо
          sleep 10
          
          # Перевіряємо
          if curl -f http://localhost:8000/health; then
            echo "✅ API працює!"
            echo "🌐 http://176.9.9.106:8000"
          else
            echo "❌ Помилка запуску"
            cat logs/app.log
            exit 1
          fi