name: Deploy Transcription API

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: root
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ secrets.SERVER_PORT }}
        timeout: 900s
        command_timeout: 600s
        script: |
          echo "🚀 Деплой Ukrainian Transcription API"
          echo "======================================"
          
          cd /root
          mkdir -p transcription-api
          cd transcription-api
          
          if [ ! -d ".git" ]; then
            echo "📥 Клонування репозиторію..."
            git clone https://github.com/WarmingZ/transcription-api.git .
          else
            echo "🔄 Оновлення коду..."
            git pull origin main
          fi
          
          echo "📦 Встановлення системних залежностей..."
          apt-get update -qq
          apt-get install -y curl git python3 python3-venv python3-dev python3-pip ffmpeg gfortran build-essential pkg-config

          echo "📦 Створення віртуального середовища..."
          if [ ! -d "venv" ]; then
            python3 -m venv venv
          fi
          
          source venv/bin/activate
          pip install --upgrade pip setuptools wheel

          echo "📚 Встановлення Python залежностей..."
          pip install numpy
          pip install torch>=2.5.0 --index-url https://download.pytorch.org/whl/cpu
          pip install torchaudio>=2.5.0 --index-url https://download.pytorch.org/whl/cpu
          
          echo "📦 Встановлення SciPy та Librosa..."
          pip install scipy --only-binary=all || pip install scipy
          pip install librosa --only-binary=all || pip install librosa
          
          echo "📦 Встановлення інших залежностей..."
          pip install webrtcvad
          pip install git+https://github.com/dofuuz/python-soxr.git
          
          echo "🔧 Виправлення конфлікту setuptools для Python 3.13..."
          pip install "setuptools<81" --force-reinstall
          
          echo "📦 Встановлення faster-whisper..."
          pip install faster-whisper[openvino]==0.10.0 || {
            echo "⚠️ Основний спосіб не спрацював, спробуємо альтернативний..."
            pip install faster-whisper==0.10.0
          }
          
          echo "📦 Встановлення решти залежностей..."
          pip install -r requirements.txt --no-deps
          pip install -r requirements.txt
          
          echo "🤖 Завантаження моделей..."
          if [ ! -d "models/faster-whisper-medium" ]; then
            echo "📥 Завантаження моделей Whisper..."
            python download_models.py
          else
            echo "✅ Моделі вже завантажені"
          fi
          
          echo "🔍 Перевірка встановлених пакетів..."
          pip list | grep -E "(torch|librosa|faster-whisper|webrtcvad|scipy|setuptools)"
          
          echo "🧪 Тестування імпорту faster-whisper..."
          python -c "import faster_whisper; print('✅ faster-whisper працює!')" || echo "❌ faster-whisper не працює!"
          
          mkdir -p logs temp
          
          echo "🛑 Зупинка старого процесу..."
          pkill -f "python main.py" || true
          sleep 3
          
          echo "🚀 Запуск API..."
          nohup venv/bin/python main.py > logs/app.log 2>&1 &
          echo $! > logs/app.pid
          
          echo "⏳ Очікування запуску (15 секунд)..."
          sleep 15
          
          echo "🔍 Перевірка статусу API..."
          if curl -f http://localhost:8000/health; then
            echo "✅ ДЕПЛОЙ УСПІШНИЙ!"
          else
            echo "❌ ПОМИЛКА ЗАПУСКУ!"
            tail -20 logs/app.log
            exit 1
          fi
