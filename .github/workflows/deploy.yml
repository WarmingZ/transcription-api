name: Deploy Transcription API

on:
  push:
    branches: [ main ]
  workflow_dispatch: # Дозволяє ручний запуск

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ secrets.SERVER_PORT }}
        timeout: 900s
        command_timeout: 600s
        script: |
          echo "🚀 Деплой Ukrainian Transcription API"
          echo "======================================"
          
          # Переходимо в системну директорію
          cd /opt
          
          # Створюємо директорію проекту
          mkdir -p transcription-api
          cd transcription-api
          
          # Клонуємо або оновлюємо репозиторій
          if [ ! -d ".git" ]; then
            echo "📥 Клонування репозиторію..."
            git clone https://github.com/WarmingZ/transcription-api.git .
          else
            echo "🔄 Оновлення коду..."
            git pull origin main
          fi
          
          # Перевіряємо наявність Python
          echo "🐍 Перевірка Python..."
          python3 --version
          
          # Встановлюємо системні залежності
          echo "📦 Встановлення системних залежностей..."
          apt-get update -qq
          apt-get install -y curl python3-pip python3-venv git
          
          # Створюємо віртуальне середовище
          echo "📦 Створення віртуального середовища..."
          if [ ! -d "venv" ]; then
            python3 -m venv venv
          fi
          
          # Активуємо venv
          source venv/bin/activate
          
          # Встановлюємо залежності
          echo "📚 Встановлення Python залежностей..."
          pip install --upgrade pip
          pip install -r requirements.txt
          
          # Завантажуємо моделі
          echo "🤖 Завантаження моделей..."
          if [ ! -d "models/faster-whisper-medium" ]; then
            python3 download_models.py
          else
            echo "✅ Моделі вже завантажені"
          fi
          
          # Створюємо директорії
          mkdir -p logs temp
          
          # Зупиняємо старий процес
          echo "🛑 Зупинка старого процесу..."
          pkill -f "python3 main.py" || true
          sleep 3
          
          # Запускаємо новий процес
          echo "🚀 Запуск API..."
          nohup python3 main.py > logs/app.log 2>&1 &
          echo $! > logs/app.pid
          
          # Чекаємо запуску
          echo "⏳ Очікування запуску (15 секунд)..."
          sleep 15
          
          # Перевіряємо статус
          echo "🔍 Перевірка статусу API..."
          if curl -f http://localhost:8000/health; then
            echo ""
            echo "✅ ДЕПЛОЙ УСПІШНИЙ!"
            echo "🌐 API доступне: http://176.9.9.106:8000"
            echo "📊 Веб-інтерфейс: http://176.9.9.106:8000"
            echo "📖 Документація: http://176.9.9.106:8000/docs"
            echo "📁 Проект: /opt/transcription-api"
            echo "📋 Логи: /opt/transcription-api/logs/app.log"
            echo ""
            echo "🔄 Управління сервісом:"
            echo "   cd /opt/transcription-api"
            echo "   pkill -f 'python3 main.py'  # зупинити"
            echo "   nohup python3 main.py > logs/app.log 2>&1 &  # запустити"
            echo ""
          else
            echo "❌ ПОМИЛКА ЗАПУСКУ!"
            echo "📋 Останні логи:"
            tail -20 logs/app.log
            echo ""
            echo "🔧 Діагностика:"
            echo "   ps aux | grep python3"
            echo "   netstat -tlnp | grep 8000"
            exit 1
          fi