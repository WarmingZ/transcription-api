name: Deploy Ukrainian Transcription API

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: root
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ secrets.SERVER_PORT }}
        timeout: 1800s
        command_timeout: 1200s
        script: |
          set -e  # Зупиняємо при помилках
          
          echo "🚀 Деплой Ukrainian Transcription API"
          echo "======================================"
          
          # Переходимо в робочу директорію
          cd /root
          rm -rf transcription-api
          mkdir -p transcription-api
          cd transcription-api
          
          # Клонуємо репозиторій
          echo "📥 Клонування репозиторію..."
          git clone https://github.com/WarmingZ/transcription-api.git .
          
          # Встановлюємо системні залежності
          echo "📦 Встановлення системних залежностей..."
          apt-get update -qq
          apt-get install -y python3 python3-pip python3-venv python3-dev ffmpeg curl git gfortran build-essential
          
          # Створюємо віртуальне середовище
          echo "📦 Створення віртуального середовища..."
          python3 -m venv venv
          source venv/bin/activate
          
          # Перевіряємо чи віртуальне середовище активоване
          if [ -z "$VIRTUAL_ENV" ]; then
            echo "❌ Віртуальне середовище не активоване!"
            exit 1
          fi
          echo "✅ Віртуальне середовище активоване: $VIRTUAL_ENV"
          
          # Оновлюємо pip
          echo "🔧 Оновлення pip..."
          pip install --upgrade pip setuptools wheel
          
          # Встановлюємо залежності поетапно
          echo "📚 Етап 1: Базові залежності..."
          pip install numpy scipy
          
          echo "📚 Етап 2: PyTorch..."
          pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
          
          echo "📚 Етап 3: ML моделі..."
          pip install faster-whisper librosa webrtcvad
          
          echo "📚 Етап 4: Web фреймворк..."
          pip install fastapi uvicorn python-multipart aiofiles httpx pydantic python-dotenv
          
          echo "📚 Етап 5: Додаткові залежності..."
          pip install scikit-learn transformers whisper-timestamped datasets accelerate language_tool_python psutil
          
          # Перевіряємо встановлені пакети
          echo "🔍 Перевірка встановлених пакетів..."
          pip list | grep -E "(torch|numpy|scipy|fastapi|faster-whisper|librosa|webrtcvad)"
          
          # Тестуємо критичні імпорти
          echo "🧪 Тестування критичних імпортів..."
          python -c "
          try:
              import torch; print('✅ PyTorch')
              import numpy; print('✅ NumPy')
              import scipy; print('✅ SciPy')
              import fastapi; print('✅ FastAPI')
              import faster_whisper; print('✅ Faster Whisper')
              import librosa; print('✅ Librosa')
              import webrtcvad; print('✅ WebRTC VAD')
              print('🎉 Всі критичні пакети працюють!')
          except Exception as e:
              print(f'❌ Помилка імпорту: {e}')
              exit(1)
          "
          
          # Завантажуємо моделі
          echo "🤖 Завантаження моделей..."
          if python download_models.py; then
            echo "✅ Моделі завантажені успішно"
          else
            echo "❌ Помилка завантаження моделей"
            exit 1
          fi
          
          # Створюємо директорії
          echo "📁 Створення директорій..."
          mkdir -p logs temp static
          
          # Зупиняємо старий процес
          echo "🛑 Зупинка старого процесу..."
          screen -S transcription-api -X quit || true
          pkill -f "python main.py" || true
          sleep 3
          
          # Перевіряємо чи існує main.py
          if [ ! -f "main.py" ]; then
            echo "❌ main.py не знайдено!"
            exit 1
          fi
          
          # Запускаємо API через screen для стабільності
          echo "🚀 Запуск API через screen..."
          
          # Встановлюємо screen якщо не встановлений
          apt-get install -y screen
          
          # Запускаємо API в screen сесії
          screen -dmS transcription-api bash -c "cd /root/transcription-api && source venv/bin/activate && python main.py > logs/app.log 2>&1"
          
          # Отримуємо PID процесу
          API_PID=$(pgrep -f "python main.py")
          echo $API_PID > logs/app.pid
          echo "✅ API запущено в screen сесії 'transcription-api' з PID: $API_PID"
          
          # Очікуємо запуску
          echo "⏳ Очікування запуску (30 секунд)..."
          sleep 30
          
          # Перевіряємо чи процес ще працює
          if ps -p $API_PID > /dev/null; then
            echo "✅ Процес API працює"
          else
            echo "❌ Процес API зупинився"
            echo "📋 Останні логи:"
            tail -50 logs/app.log
            echo "📋 Screen сесії:"
            screen -ls || echo "Screen не встановлений"
            exit 1
          fi
          
          # Перевіряємо статус API
          echo "🔍 Перевірка статусу API..."
          for i in {1..5}; do
            echo "Спроба $i/5..."
            if curl -f http://localhost:8000/health > /dev/null 2>&1; then
              echo "✅ API відповідає!"
              break
            else
              echo "⏳ Очікування відповіді API..."
              sleep 10
            fi
          done
          
          # Фінальна перевірка
          if curl -f http://localhost:8000/health; then
            echo ""
            echo "🎉 ДЕПЛОЙ УСПІШНИЙ!"
            echo "🌐 API доступний на: http://localhost:8000"
            echo "📖 Документація: http://localhost:8000/docs"
            echo "🖥️ Веб-інтерфейс: http://localhost:8000/static/index.html"
            echo "📋 PID процесу: $API_PID"
            echo "🖥️ Screen сесія: transcription-api"
            echo "📁 Логи: logs/app.log"
            echo ""
            echo "💡 Корисні команди:"
            echo "   screen -r transcription-api  # Підключитися до сесії"
            echo "   screen -ls                   # Показати всі сесії"
            echo "   screen -S transcription-api -X quit  # Зупинити сесію"
          else
            echo "❌ ПОМИЛКА ЗАПУСКУ!"
            echo "📋 Останні логи:"
            tail -50 logs/app.log
            echo ""
            echo "📋 Статус процесу:"
            ps aux | grep "python main.py" | grep -v grep || echo "Процес не знайдено"
            exit 1
          fi