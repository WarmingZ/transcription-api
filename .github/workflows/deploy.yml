name: Deploy Ukrainian Transcription API

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: root
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ secrets.SERVER_PORT }}
        timeout: 1200s
        command_timeout: 900s
        script: |
          echo "🚀 Деплой Ukrainian Transcription API"
          echo "======================================"
          
          # Переходимо в робочу директорію
          cd /root
          mkdir -p transcription-api
          cd transcription-api
          
          # Оновлюємо код
          if [ ! -d ".git" ]; then
            echo "📥 Клонування репозиторію..."
            git clone https://github.com/WarmingZ/transcription-api.git .
          else
            echo "🔄 Оновлення коду..."
            git pull origin main
          fi
          
          # Перевіряємо версію системи
          echo "🔍 Перевірка системи..."
          cat /etc/os-release
          python3 --version
          
          # Встановлюємо системні залежності для Debian
          echo "📦 Встановлення системних залежностей..."
          apt-get update -qq
          apt-get install -y \
            curl \
            git \
            python3 \
            python3-venv \
            python3-dev \
            python3-pip \
            ffmpeg \
            gfortran \
            build-essential \
            pkg-config \
            libsndfile1-dev \
            libasound2-dev \
            portaudio19-dev \
            libffi-dev \
            libssl-dev \
            zlib1g-dev \
            libbz2-dev \
            libreadline-dev \
            libsqlite3-dev \
            libncurses5-dev \
            libncursesw5-dev \
            xz-utils \
            tk-dev \
            libxml2-dev \
            libxmlsec1-dev \
            libffi-dev \
            liblzma-dev
          
          # Створюємо віртуальне середовище
          echo "📦 Створення віртуального середовища..."
          if [ ! -d "venv" ]; then
            python3 -m venv venv
          fi
          
          # Активуємо віртуальне середовище
          source venv/bin/activate
          
          # Оновлюємо pip та базові інструменти
          echo "🔧 Оновлення pip та інструментів..."
          pip install --upgrade pip wheel
          pip install "setuptools<81"  # Виправляємо конфлікт з webrtcvad
          
          # Встановлюємо Python залежності поетапно
          echo "📚 Етап 1: Базові залежності..."
          pip install numpy==1.24.3 || pip install numpy
          pip install scipy==1.11.4 || pip install scipy
          
          echo "📚 Етап 2: PyTorch..."
          pip install torch==2.1.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cpu || {
            echo "⚠️ PyTorch з CPU не встановився, спробуємо без CPU..."
            pip install torch torchaudio
          }
          
          echo "📚 Етап 3: Аудіо обробка..."
          pip install librosa==0.10.1 || pip install librosa
          
          # Встановлюємо webrtcvad з обробкою попередження
          echo "📦 Встановлення webrtcvad..."
          pip install webrtcvad==2.0.10 || pip install webrtcvad
          
          pip install git+https://github.com/dofuuz/python-soxr.git || {
            echo "⚠️ soxr не встановився, пропускаємо..."
          }
          
          echo "📚 Етап 4: ML моделі..."
          pip install faster-whisper==0.10.0 || pip install faster-whisper
          pip install speechbrain==0.5.16 || pip install speechbrain
          pip install transformers==4.35.2 || pip install transformers
          pip install scikit-learn==1.3.2 || pip install scikit-learn
          
          echo "📚 Етап 5: Додаткові залежності..."
          pip install whisper-timestamped==1.14.2 || pip install whisper-timestamped
          pip install datasets==2.14.6 || pip install datasets
          pip install accelerate==0.24.1 || pip install accelerate
          pip install language_tool_python==2.7.1 || {
            echo "⚠️ language_tool_python не встановився, пропускаємо..."
          }
          
          echo "📚 Етап 6: Web фреймворк..."
          pip install fastapi==0.104.1 || pip install fastapi
          pip install uvicorn[standard]==0.24.0 || pip install uvicorn
          pip install python-multipart==0.0.6 || pip install python-multipart
          pip install aiofiles==23.2.1 || pip install aiofiles
          pip install httpx==0.25.2 || pip install httpx
          pip install pydantic==2.5.0 || pip install pydantic
          pip install python-dotenv==1.0.0 || pip install python-dotenv
          pip install psutil==5.9.6 || pip install psutil
          
          # Перевіряємо встановлені пакети
          echo "🔍 Перевірка встановлених пакетів..."
          pip list | grep -E "(torch|librosa|faster-whisper|webrtcvad|scipy|fastapi|speechbrain|setuptools)"
          
          # Перевіряємо версію setuptools
          echo "🔍 Версія setuptools:"
          pip show setuptools | grep Version
          
          # Тестуємо критичні імпорти
          echo "🧪 Тестування критичних імпортів..."
          python -c "
          import sys
          failed_imports = []
          
          try:
              import torch; print('✅ torch')
          except Exception as e:
              print(f'❌ torch: {e}')
              failed_imports.append('torch')
              
          try:
              import torchaudio; print('✅ torchaudio')
          except Exception as e:
              print(f'❌ torchaudio: {e}')
              failed_imports.append('torchaudio')
              
          try:
              import faster_whisper; print('✅ faster-whisper')
          except Exception as e:
              print(f'❌ faster-whisper: {e}')
              failed_imports.append('faster-whisper')
              
          try:
              import librosa; print('✅ librosa')
          except Exception as e:
              print(f'❌ librosa: {e}')
              failed_imports.append('librosa')
              
          try:
              import webrtcvad; print('✅ webrtcvad')
          except Exception as e:
              print(f'❌ webrtcvad: {e}')
              failed_imports.append('webrtcvad')
              
          try:
              import scipy; print('✅ scipy')
          except Exception as e:
              print(f'❌ scipy: {e}')
              failed_imports.append('scipy')
              
          try:
              import fastapi; print('✅ fastapi')
          except Exception as e:
              print(f'❌ fastapi: {e}')
              failed_imports.append('fastapi')
              
          try:
              import speechbrain; print('✅ speechbrain')
          except Exception as e:
              print(f'❌ speechbrain: {e}')
              failed_imports.append('speechbrain')
          
          if failed_imports:
              print(f'❌ Не вдалося імпортувати: {failed_imports}')
              sys.exit(1)
          else:
              print('🎉 Всі критичні пакети працюють!')
          " || {
            echo "❌ Деякі імпорти не працюють, але продовжуємо..."
          }
          
          # Спеціальний тест для webrtcvad
          echo "🧪 Тестування webrtcvad..."
          python -c "
          import warnings
          warnings.filterwarnings('ignore', category=UserWarning)
          try:
              import webrtcvad
              vad = webrtcvad.Vad(2)
              print('✅ webrtcvad працює правильно')
          except Exception as e:
              print(f'❌ webrtcvad помилка: {e}')
          " || echo "⚠️ webrtcvad має проблеми, але продовжуємо..."
          
          # Завантажуємо моделі
          echo "🤖 Завантаження моделей..."
          if [ ! -d "models/faster-whisper-medium" ]; then
            echo "📥 Завантаження faster-whisper моделей..."
            python download_models.py
          else
            echo "✅ Моделі вже завантажені"
          fi
          
          # Створюємо необхідні директорії
          mkdir -p logs temp static
          
          # Зупиняємо старий процес
          echo "🛑 Зупинка старого процесу..."
          pkill -f "python main.py" || true
          sleep 3
          
          # Запускаємо API
          echo "🚀 Запуск API..."
          nohup venv/bin/python main.py > logs/app.log 2>&1 &
          echo $! > logs/app.pid
          
          # Очікуємо запуску
          echo "⏳ Очікування запуску (20 секунд)..."
          sleep 20
          
          # Перевіряємо статус
          echo "🔍 Перевірка статусу API..."
          if curl -f http://localhost:8000/health; then
            echo ""
            echo "✅ ДЕПЛОЙ УСПІШНИЙ!"
            echo "🌐 API доступний на: http://localhost:8000"
            echo "📖 Документація: http://localhost:8000/docs"
            echo "🖥️ Веб-інтерфейс: http://localhost:8000/static/index.html"
          else
            echo "❌ ПОМИЛКА ЗАПУСКУ!"
            echo "📋 Останні логи:"
            tail -30 logs/app.log
            exit 1
          fi